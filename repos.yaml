repos:
- id: /.*/
  allow_custom_workflows: true
  apply_requirements:
  - approved
workflows:
  default:
    plan:
      steps:
      - init
      - run: terraform fmt -check -diff
      - plan
    apply:
      steps:
      - apply
